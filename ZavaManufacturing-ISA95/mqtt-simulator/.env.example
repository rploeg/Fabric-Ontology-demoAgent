# =============================================================================
# Zava MQTT Simulator — Deployment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# Used by scripts/deploy.sh and VS Code tasks.
#
#   cp .env.example .env
#   vi .env
# =============================================================================

# Azure Container Registry (just the name, without .azurecr.io)
ACR_NAME=myacr

# AKS cluster
AKS_RG=my-resource-group
AKS_NAME=my-aks-cluster

# Image settings (optional — defaults are sensible)
# IMAGE_NAME=zava-simulator
# IMAGE_TAG=latest          # defaults to git short-SHA
# K8S_NAMESPACE=zava-simulator

# =============================================================================
# Event Hub — Service Principal (for Docker-local Event Hub testing)
# =============================================================================
# When outputMode=eventHub and credential=defaultCredential, the Azure SDK's
# EnvironmentCredential picks up these env vars inside the Docker container.
#
# Create a service principal:
#   az ad sp create-for-rbac --name "zava-simulator" --skip-assignment
#
# Assign the Data Sender role on your Event Hub namespace:
#   az role assignment create \
#     --role "Azure Event Hubs Data Sender" \
#     --assignee <appId> \
#     --scope /subscriptions/<sub>/resourceGroups/<rg>/providers/Microsoft.EventHub/namespaces/<ns>
#
# Then fill in the values below:
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
